import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dumb-loading-skeletons',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
